<!DOCTYPE html>
<html>
  <head>
    <title>Spaces | ruBnB</title>
  </head>
  <body>
    <h1>ruBnB</h1>
    <% if session[:user] == nil %>
      <div><h3>Nobody's logged in</h3></div>
    <% else %>
      <div><h3>Welcome <%= session[:user].name %>!</h3></div>
    <% end %>
    <h2>Here are all the spaces:</h2>
    <% @spaces.each do |space| %>
      <div>
        <h3><a href="/spaces/<%=space.id%>"><%= space.name %></a></h3>
        About this space: <%= space.description %> <br>
        Price per night: £<%= space.price_per_night %> <br>
        Currently available? <%= space.availability == "true" ? "Yes" : "No" %> 
      </div>
    <% end %>
  </body>
</html>